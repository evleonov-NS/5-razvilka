# Развилка, v0.1.0

# Этап 4 — сценарии + pre-mortem
Ядро создания решения: промпт 9.1 → валидация → транзакция Decision + Scenario[3] + FailureMode[3–5]; экран `/decisions/[id]` показывает разбор из БД.

# Среда разработки
- [x] Cursor (Agent / Composer)
- [ ] GPT Code / ChatGPT
- [ ] Claude Code (terminal)
- [ ] Claude (web)
- [ ] Другое: ___________

Версии:
- Node.js: (локально)
- npm: (локально)
- Ключевые пакеты: Next.js 15.5, Prisma 6, next-auth, openai, Zod, Tailwind

# Дата реализации
2026-07-26 ~14:00 — ~14:40
Ориентировочное затраченное время: ~1–1.5 ч (агент + ручная проверка)

# Тип изменений
- [x] Только приложение (frontend/app)
- [ ] Только сервер (backend/server/VPS)
- [x] И приложение, и сервер
- [ ] Инфраструктура / DevOps
- [ ] База данных
- [ ] Конфиги / окружение
- [ ] Автотесты (unit / E2E)
- [x] Документация
- [ ] Другое:

# Затраченное время
- Общее время: ~1–1.5 ч
- Примерно по этапам:
  - API + промпт + транзакция: ~40 мин
  - UI экрана результата: ~20 мин
  - build / docs / проверка: ~30 мин

# Git
- Ветка: main
- Коммиты (ключевые): этап 4 (этот коммит)
- Запушено на remote: [x] да  [ ] нет
- Force-push: [ ] да  [x] нет
- Незакоммиченные файлы: —

# Тесты
- Unit: нет
- E2E: нет
- Ручная проверка: `/decisions/new` → разбор «проверка» → 3 сценария + pre-mortem
- `npm run build` — пройден (после остановки `dev`)

# Команды проекта
| Команда | Назначение |
|---------|------------|
| `npm run dev` | Локальный сервер :3015 |
| `npm run build` | prisma generate && next build |
| `npm run llm:verify` | Smoke LLM-слоя |

# Что сделано
- `POST /api/decisions`: resolveLlmCredentials → промпт 9.1 → parseJsonSafe → ScenarioResponseSchema → транзакция
- Невалидный LLM: без записи в БД, лог raw, 502/422
- recordLlmUsage; consumePlatformCredit только PLATFORM + не-owner
- `/decisions/[id]`: ScenarioCard, FailureModeList, LikelihoodBadge
- lib/prompts.ts; ADR-023
- Документы: STATUS, CHANGELOG, PLAN, DECISIONS, PROMPT.md, dev-log

# Что изменилось
- **Функции / поведение:** создание решения генерирует сценарии и pre-mortem; старые пустые карточки показывают «Разбор ещё не готов»
- **Файлы / модули:** `app/api/decisions/route.ts`, `app/decisions/[id]/page.tsx`, `lib/prompts.ts`, компоненты Scenario*
- **Улучшено:** кредит списывается только после успеха
- **Исправлено:** пустые Decision без LLM больше не создаются новым потоком

# Изменения на сервере
- Vercel: деплой после push; **LLM-ключи в Vercel — чеклист этапа 9** (см. PLAN § этап 9)
- Миграции: новых в этапе 4 нет (`user_llm_settings` уже была)

# Изменения в приложении
- UI/UX: экран результата со сценариями и pre-mortem
- Логика: генерация до сохранения (ADR-023)
- API: полноценный POST /api/decisions с LLM
- БД: Scenario + FailureMode пишутся при успехе
- Техдолг: GET /api/decisions не обязателен (чтение через RSC); дерево/ревью — этапы 7–8

# Причина изменений
- Зачем: закрыть критический путь «описал → увидел сценарии»
- Какую проблему решает: без этапа 4 продукт — только форма и пустые карточки

# Связь с чатами
- Чат / сессия: этап 4 (Cursor)
- Продолжение от: этап 3 (fa3e83f)
- Следующий чат (тема): этап 5 — полировка экрана результата; затем 7 / 8 / 9

# Следующий шаг
- Этап 5: кнопка «Отметить исход», Loading/Error, полировка UI
- Этап 7: дерево развилок; этап 8: RESOLVED + ревью
- Этап 9: полировка + **внести LLM API-ключи в Vercel** (см. PLAN)
- Этап 2а: демо-кнопки в настройках (параллельно)

# Чеклист «готово к сдаче / закрытию этапа»
- [x] Функционал работает в браузере
- [x] Проверка вручную пройдена
- [x] `npm run build` проходит
- [ ] Unit-тесты проходят, если добавлены
- [ ] E2E-тесты проходят, если добавлены
- [x] Нет незакоммиченных критичных файлов
- [x] Документация / dev-log обновлены
- [x] Известные баги зафиксированы (старые пустые решения без сценариев — ожидаемо)

# Технические заметки
- Статус OPEN→RESOLVED только на этапе 8
- На Windows не запускать `build` параллельно с `dev` (EPERM Prisma DLL)
- Production без `DEEPSEEK_API_KEY` в Vercel не сможет генерировать на платформенном ключе

# Артефакты
- Production: https://5-razvilka.vercel.app
- Локально: http://localhost:3015
- Dev-log: этот файл

# Статус этапа
- [ ] В процессе
- [x] Завершено
- [ ] Перенесено в следующий чат
- [ ] Требует доработки

---
*Пишите кратко, структурно, без воды — как журнал senior-разработчика.*
