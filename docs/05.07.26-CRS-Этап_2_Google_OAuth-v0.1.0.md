# Развилка, v0.1.0

# Этап 2: Auth.js + Google OAuth (локально и Vercel)
Вход только через Google; сессия в Neon (database strategy); настройка Google Console и env.

# Среда разработки
- [x] Cursor (Agent / Composer)
- [ ] GPT Code / ChatGPT
- [ ] Claude Code (terminal)
- [ ] Claude (web)
- [ ] Другое: ___________

Версии:
- Node.js: v22+
- npm: v11+
- Ключевые пакеты: next-auth 5.0.0-beta.31, @auth/prisma-adapter 2.11, Prisma 6

# Дата реализации
2026-06-30 — 2026-07-05 (несколько сессий Cursor)
Ориентировочное затраченное время: ~4–6 ч (код + Google + Vercel отладка)

# Тип изменений
- [x] И приложение, и сервер
- [x] База данных
- [x] Конфиги / окружение
- [x] Документация
- [x] Dev-утилита view-db (отдельная сессия)

# Затраченное время
- Общее время: ~4–6 ч
- Примерно по этапам:
  - view-db + статус проекта: ~1.5 ч
  - Промпт 2 (код auth, миграция): ~1.5 ч
  - Google Console + .env + Vercel (redirect_uri, AUTH_URL, env): ~2 ч

# Git
- Ветка: main
- Коммиты (ключевые):
  - `36a9451` — view-db
  - `8e9a5bd` — Этап 2 Auth.js + Google OAuth
  - (этот коммит) — docs AUTH_GOOGLE_VERCEL + dev-log
- Запушено на remote: [x] да
- Force-push: [ ] нет
- Незакоммиченные файлы: `.env`, `PROMPT.md` (корень, черновик)

# Тесты
- Unit / E2E: не добавлены
- Ручная проверка: [x] localhost:3000 — вход Google OK
- [x] https://5-razvilka.vercel.app — вход Google OK (после настройки env)
- [x] `npm run build` — проходит

# Команды проекта
| Команда | Назначение |
|---------|------------|
| `npm run dev` | Локальный сервер |
| `npm run build` | prisma generate + next build |
| `npx prisma migrate deploy` | Миграции на Neon |
| `npm run db:seed` | Демо-данные |
| `npm run db:verify` | Smoke доменной схемы |

# Что сделано
- **view-db:** `/view-db` — CRUD таблиц Neon, local/prod профили
- **Auth.js v5:** `auth.ts`, Prisma Adapter, Google provider only
- **Prisma:** миграция `auth_google` — Account, Session, VerificationToken; User без passwordHash
- **UI:** `/login`, Header (аватар, выход), GoogleSignInButton
- **lib/auth.ts:** getCurrentUser, requireUser
- **middleware:** заготовка защиты `/decisions/*`
- **Документация:** PROMPTS/PLAN/DECISIONS (ADR-003), AUTH_SETUP, **AUTH_GOOGLE_VERCEL** (полная инструкция)
- **Production:** Google OAuth + env на Vercel отлажены

# Что изменилось
- **Функции / поведение:** вход через Google; сессия в httpOnly cookie + Neon Session
- **Файлы / модули:** auth.ts, app/api/auth/[...nextauth], components/Header, middleware.ts
- **Улучшено:** docs по OAuth (типичные ошибки redirect_uri, AUTH_URL local vs Vercel)
- **Исправлено:** redirect_uri_mismatch (3 URI в Google); Server configuration (AUTH_SECRET на Vercel); AUTH_URL=`KEY=` в Value Vercel

# Изменения на сервере
- Neon: миграция `20250705180000_auth_google`
- Vercel env: DATABASE_*, AUTH_SECRET, AUTH_URL, GOOGLE_*
- Production URL: https://5-razvilka.vercel.app
- Google OAuth client: razvilka-local

# Изменения в приложении
- UI/UX: кнопка «Войти через Google», шапка с пользователем
- Логика: Auth.js database sessions
- API: `/api/auth/[...nextauth]`
- БД: User + Account + Session (Auth.js schema)

# Причина изменений
- Этап 2 MVP: авторизация без email/пароля — только Google (ADR-003)
- Prisma Adapter — пользователи и сессии в своей Neon

# Связь с чатами
- Чат / сессия: Cursor, июнь–июль 2026 (несколько сессий)
- Продолжение от: Этап 1 (доменная схема)
- Следующий чат (тема): **Этап 3 — LLM-слой** (Промпт 3)

# Следующий шаг
- Промпт 3: `lib/llm.ts`, `lib/json.ts`, `lib/validators.ts`, OpenAI env
- Этап 4: POST `/api/decisions` — создание решения с LLM

# Чеклист «готово к сдаче / закрытию этапа»
- [x] Функционал работает в браузере (local + Vercel)
- [x] `npm run build` проходит
- [x] Документация / dev-log обновлены
- [ ] Unit/E2E тесты (не в scope этапа)

# Технические заметки
- **Баги / инциденты:** redirect_uri_mismatch — git-main vs vercel.app; Vercel Value с `AUTH_URL=`; отсутствие AUTH_SECRET на Vercel
- **Ограничения:** Google app в Testing — только Test users; view-db на prod выключен
- **Идеи:** привязать Decision к userId на главной (этап 6); обновить README
- **Не делать:** не ставить production AUTH_URL в локальный `.env`; не коммитить `.env`

# Артефакты
- Production: https://5-razvilka.vercel.app
- Локально: http://localhost:3000
- Инструкция: docs/AUTH_GOOGLE_VERCEL.md
- Google: проект razvilka, OAuth client razvilka-local

# Статус этапа
- [ ] В процессе
- [x] Завершено
- [ ] Перенесено в следующий чат
- [ ] Требует доработки
