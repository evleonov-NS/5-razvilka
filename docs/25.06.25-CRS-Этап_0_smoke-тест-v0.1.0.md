# Развилка, v0.1.0

# Этап 0: каркас Next.js + Prisma, Neon, деплoy Vercel
Smoke-тест: главная читает заметки `Note` из PostgreSQL (Neon) локально и на Vercel.

# Среда разработки
- [x] Cursor (Agent / Composer)
- [ ] GPT Code / ChatGPT
- [ ] Claude Code (terminal)
- [ ] Claude (web)
- [ ] Другое: ___________

Версии:
- Node.js: v22+
- npm: v10+
- Ключевые пакеты: Next.js 15, Prisma 6, Tailwind 4

# Дата реализации
2026-06-25 (сессия Cursor)
Ориентировочное затраченное время: ~3–4 ч (план, каркас, Neon, Vercel, отладка деплоя)

# Тип изменений
- [x] И приложение, и сервер
- [x] Инфраструктура / DevOps
- [x] База данных
- [x] Конфиги / окружение
- [x] Документация

# Затраченное время
- Общее время: ~3–4 ч
- Примерно по этапам:
  - план и docs/: ~45 мин
  - каркас Next.js + Prisma: ~1 ч
  - Neon + .env + migrate/seed: ~30 мин
  - Vercel (ошибка старого коммита, redeploy): ~45 мин

# Git
- Ветка: main
- Коммиты (ключевые): `560546d` план → `02ea110` Этап 0 → `5b4af00` триггер Vercel
- Запушено на remote: [x] да
- Force-push: [ ] нет
- Незакоммиченные файлы: docs/ (сводка), `.env` (не в git)

# Тесты
- Unit / E2E: не добавлены
- Ручная проверка: [x] локально localhost:3000 — 3 заметки
- [x] Vercel production (`git-main`) — OK
- [x] `npm run build` — проходит

# Команды проекта
| Команда | Назначение |
|---------|------------|
| `npm run dev` | Локальный сервер |
| `npm run build` | prisma generate + next build |
| `npx prisma migrate deploy` | Миграции на Neon |
| `npm run db:seed` | prisma db seed (3 заметки) |

# Что сделано
- План MVP: docs/PLAN, STATUS, CHANGELOG, PROMPTS, DECISIONS, правила Cursor
- Next.js App Router + TypeScript + Tailwind + Prisma 6
- Модель `Note`, миграция `init_note`, seed
- `app/page.tsx` — Server Component, список заметок из Neon
- `lib/prisma.ts`, `lib/version.ts`
- Подключение Neon (DATABASE_URL pooled, DIRECT_URL без `-pooler`)
- Деплой Vercel: env, успешный production на `git-main`
- Правило **kip** (commit + push: заголовок + тело)
- Fix: `db:seed` через `prisma db seed` (tsx не грузил `.env`)

# Что изменилось
- **Функции / поведение:** smoke-тест чтения БД на главной
- **Файлы / модули:** app/, lib/, prisma/, package.json
- **Улучшено:** README — инструкции Neon/Vercel (PowerShell)
- **Исправлено:** seed без DATABASE_URL; Vercel redeploy старого коммита `560546d`

# Изменения на сервере
- Vercel: проект `5-razvilka`, Production Ready (`5b4af00`)
- Neon: таблица `Note`, seed 3 записи
- URL: `5-razvilka-git-main-evgenyleonovns.vercel.app`

# Изменения в приложении
- UI/UX: минимальная главная «Развилка», список заметок, футер с версией
- БД: PostgreSQL Neon через Prisma
- TODO: удалить `Note` на Этапе 1 (доменная схема)

# Причина изменений
- Зачем: обязательный smoke-тест деплоя (PROJECT.md §17) перед доменной разработкой
- Проблема: проверить Neon + Vercel + Prisma end-to-end

# Связь с чатами
- Чат / сессия: Cursor Agent, проект 5-razvilka
- Следующий чат (тема): Этап 1 — доменная схема (User, Decision, Scenario, FailureMode)

# Следующий шаг
- Промпт 1: заменить `Note` на доменную схему из PROJECT.md §6
- Миграция `domain_init`, обновить seed (демо-кейс §16)

# Чеклист «готово к сдаче / закрыtiю этапа»
- [x] Функционал работает в браузере (local + Vercel)
- [x] Проверка вручную пройдена
- [x] `npm run build` проходит
- [ ] Unit/E2E — не в scope Этапа 0
- [x] Документация обновлена (сводка)
- [x] Известные баги: 3 failed деплоя Vercel (старый коммит) — не удаляются на Hobby, игнорировать

# Технические заметки
- Vercel Redeploy старого деплоя ≠ последний commit с GitHub
- Deployment URL (`88ryaxp7z`) — снимок; `git-main` — актуальный production
- Seed: только `prisma db seed`, не `tsx prisma/seed.ts` напрямую

# Артефакты
- GitHub: https://github.com/evleonov-NS/5-razvilka
- Production: https://5-razvilka-git-main-evgenyleonovns.vercel.app/

# Статус этапа
- [ ] В процессе
- [x] Завершено
- [ ] Перенесено в следующий чат
- [ ] Требует доработки
